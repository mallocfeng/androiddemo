import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gov.service.portal',
  appName: '国家政务服务',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
